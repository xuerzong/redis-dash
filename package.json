{
  "name": "redis-dash",
  "version": "0.0.0",
  "description": "This is a lightweight Redis Manager.",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev --workspace=app",
    "build:app": "npm run build --workspace=app",
    "build:cli": "npm run build --workspace=cli",
    "build:cli:npm": "RDS_SKIP_NATIVE_BUILD=1 npm run build --workspace=cli",
    "build:web": "npm run build --workspace=web",
    "build": "npm-run-all clean:* build:app build:cli",
    "copy:app": "mkdir -p ./cli/dist && cp -r ./app/dist ./cli/dist/app",
    "copy:readme": "cp README.md ./cli",
    "package:binary": "node ./scripts/package-binary.mjs",
    "clean:cli": "rimraf ./cli/dist",
    "clean:app": "rimraf ./app/dist",
    "publish:cli": "npm-run-all build copy:* package:binary && npm publish --workspace=cli",
    "release:npm": "npm-run-all clean:* build:app copy:app copy:readme build:cli:npm",
    "format": "prettier --write .",
    "release": "npm-run-all build copy:* package:binary",
    "prepare": "husky",
    "lint": "lint-staged",
    "desktop:dev": "npm run tauri dev --workspace=desktop",
    "desktop:build": "npm run tauri build --workspace=desktop",
    "replace-version": "node scripts/replace-version.mjs"
  },
  "workspaces": [
    "cli",
    "app",
    "desktop",
    "packages/*",
    "web"
  ],
  "private": true,
  "author": "xuerzong@gmail.com",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.2.0",
    "esbuild": "^0.25.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
