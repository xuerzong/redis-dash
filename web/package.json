{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "stropress dev --dir docs",
    "build": "stropress build --dir docs",
    "preview": "stropress dev --dir docs"
  },
  "devDependencies": {
    "copy-to-clipboard": "^3.3.3",
    "stropress": "^0.0.7"
  }
}
